---
name: create-skill
description: Create a new Agent Skill (SKILL.md) with correct frontmatter and layout.
---

# Create Skill

Create `skill-name/SKILL.md` with YAML frontmatter name+description. Install to `~/.piwin/skills/<name>/` or `.pi/skills/<name>/`.
