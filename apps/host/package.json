{
  "name": "@piwin/host-app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "piwin-host": "./src/index.ts"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "echo \"lint ok (host-app)\""
  },
  "dependencies": {
    "@piwin/host-runtime": "workspace:*",
    "@piwin/host-server": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
