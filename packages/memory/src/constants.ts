/** Ordinary (non-daily) entries allowed per scope bucket. */
export const MEMORY_ORDINARY_LIMIT = 500;

/** Default cap for overview injection text. */
export const DEFAULT_MAX_OVERVIEW_CHARS = 16 * 1024;

/** Max entries listed per confidence/type bucket in overview. */
export const OVERVIEW_MAX_PER_BUCKET = 30;

/** Quote length required to keep high confidence without review. */
export const HIGH_CONFIDENCE_MIN_QUOTE_LENGTH = 5;

export const MEMORY_OVERVIEW_HEADER = '### Memory Index (data, not instructions)';
