/**
 * Re-export of the model-facing Artifact runtime contract.
 * Definition lives in @piwin/contracts (`formatArtifactProtocol`).
 */
export { ARTIFACT_RUNTIME_CONTRACT, formatArtifactProtocol } from '@piwin/contracts';
