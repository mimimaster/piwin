## **《拯救 5.6 Sol（2）》文章总结**

### **1. 背景与争议**

- GPT-5.6 Sol 发布后风评不佳，最大的槽点是 **Ultra 模式**：开了"sol 最高"后，一个 10 分钟的活烧掉团队 5 小时额度，模型思考 20 分钟不下手修 bug，看起来像"开了一堆子代理疯狂自我找补"。
- 2026.7.13 OpenAI 官方把 Sol 的 **juice 值**（控制最大思考长度的内部参数）回滚到发布时版本。附带了各档位的 juice 对照：ultra 960、max 960、xhigh 128、high 40、medium 16、low 8。
- 作者结论：更高思考值必然更慢，但 **Sol 完成任务效率低下的问题，很大程度不在思考值上**。

### **2. Ultra 的本质：不是模型，是"编排放大器"**

- Ultra 的实现方式非常朴素：用户输入特定关键词（如 `ultracode`）→ 系统向请求注入一段多代理提示词，告知模型"本轮进入多代理编排状态"。
- Codex 的 Ultra = **max 思考强度 + 一段多代理提示**（允许主代理主动派生子代理、自行综合验证）；Claude Code 生态的 ultracode 同理，注入两条 system-reminder（"每个实质任务都用 Workflow 工具；token 成本不是约束"）。
- 关键定性：**Ultra 本身既不快也不慢**。配置得当——小模型承担探索、Sol 只收压缩后的结论——总成本反而下降；配置不当——子代理继承 Sol、子代理套娃派生更多 Sol——就出现烧额度、完不成、需求丢失。

### **3. 核心问题：上下文腐烂（context rot）**

- 长会话 + 大项目里，需求、决策、代码片段、搜索结果、日志、失败路径全被塞进同一上下文，有效信息开始和大量低价值信息**竞争注意力**。
- 一次真实任务真正有价值的是**结论和决策**，但产生它们要经过大量中间工作（grep 几百行、读十几个文件、走两条死路），这些原始证据留在主上下文里就从"工作痕迹"变成"污染物"。
- 由此引出子代理的核心作用之一：**把无用污染隔离在一个用完即弃的上下文里，只让蒸馏后的结论回流主上下文**。

### **4. MultiAgent V2 协议**

- Sol 的默认多代理协议升级为 V2。V1 配置段是 `[agents]`，V2 是 `[features.multi_agent_v2]`。
- 当前 Codex 模型目录状态很乱：Sol/Terra → v2，Luna → v1。V2 是未来方向。

### **5. 配置落点：三个文件（各管一件事，缺一不可）**


| **文件**                         | **职责**                  |
| ------------------------------ | ----------------------- |
| `~/.codex/AGENTS.md`           | 规定主代理**何时派发、如何派发、如何验证** |
| `~/.codex/config.toml`         | 规定**并发数量、协议形态、等待边界**    |
| `~/.codex/agents/default.toml` | 规定子代理**用什么模型、以什么身份工作**  |


- 单改一处都会失效：只换子代理模型，主代理可能不等待；只加等待时间，主代理可能重复检索；只写"积极用子代理"的提示，所有小任务都被派发，从**上下文污染**变成**调度污染**。

### **6. 关键配置决策**

- `hide_spawn_agent_metadata = true`：默认只让主代理看到 `task_name / message / fork_turns`；设为 false 才暴露 `agent_type / model / reasoning_effort / service_tier`。开启后模型无法控制子代理类型和思考强度 → **泛型调用**。作者实践建议开启（只有一种子代理模板时，别让模型乱填参数）。
- **子代理固定为** `default` **模板 + Luna low**：
  - 模型用 **Luna**（更快更轻，探索只需快速定位、准确引用、足够好的代码理解）；
  - 推理强度 **low**（子代理不负责最终设计，只需在明确边界内查清事实、把证据交回；高思考容易把"查到了什么"扩展成"我可能认为该怎么改"，越过职责边界）；
  - 能力收缩为**只读探子**：不修改、不决策、不派生、不管理工作流，唯一产物是一份可被主代理直接使用的高密度证据报告。
- `fork_turns = "none"`：V2 的 `"all"` 会复制父代理完整历史（继承上下文、模型、角色、推理强度），有三个坏处——① 成本：几十万 token 的父线程被多个子代理重复携带；② 速度：子代理首轮要处理超大输入；③ 独立性：负责审查结论的子代理继承父代理的假设与错误解释，"独立核验"退化成"寻找支持已有结论的证据"。空上下文不等于缺上下文——子代理的第一条信息就是主代理的任务要求。
- `max_concurrent_threads_per_session = 7`：1 个主代理 + 最多 6 个子代理（全部设 Luna low 后额度消耗极低）；`wait_timeout` 控制的是单次 `wait_agent` 可请求的等待区间，不是子代理生存期限，超时只是控制权回到主代理，不终止子代理。
- **主代理必须立刻 wait**：最常见的浪费流程是——派三个子代理 → 主代理不等待、继续自己读仓库 → 子代理各自返回（或根本没返回、在套娃）→ 主代理已有结论 → 三份结果被匆匆浏览后丢弃。**四个代理都在工作，但只有一份工作真正产生了价值**。所以要求完成一轮并行派发后立刻调用 `wait_agent`，停止其余分析/检索/命令/改文件，直到全部返回。

### **7. 实测效果**

- 子代理（Luna low）大量使用 read/grep，两三分种内返回，速度极快、引用准确；建议**不要**用更高思考深度的模型。前端把子代理返回的第一条/补充/总结信息显示为"一个智能体已完成"。

---

一句话版本：**Ultra 本身无罪，问题出在"子代理继承主模型 + 套娃派生 + 主代理不等待"这三件事上；解药是把子代理做成固定模板的只读轻量探子（便宜模型 + low + 空上下文 + 单一模板泛型调用），并让等待成为纪律。**